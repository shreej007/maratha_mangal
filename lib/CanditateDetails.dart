class CandidateDetails{
  var dob;
  var name;
  double height;
  var occupation;
  var location;
  var education;
  var id;
  var photo;

  CandidateDetails(this.dob, this.name, this.height, this.occupation,
      this.location, this.education, this.id, this.photo);
}